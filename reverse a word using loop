#include <iostream>
#include <string>

int main() {
  std::string word;
  std::cout << "Enter a word: ";
  std::cin >> word;

  for (int i = word.length() - 1; i >= 0; i--) {
    std::cout << word[i];
  }

  return 0;
}
